{
  "name": "the-hunt-site",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "astro check && vitest run tests/unit",
    "test:build": "astro build && vitest run tests/build"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "@fontsource/cinzel": "^5.0.0",
    "@fontsource/ibm-plex-mono": "^5.0.0",
    "@fontsource/literata": "^5.0.0",
    "@fontsource/rubik-glitch": "^5.0.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@types/node": "^26.4.1",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
